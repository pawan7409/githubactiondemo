name: Greetings
on: [push,workflow_dispatch]
# on:
#     - push
#     - workflow_dispatch
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: "get code "
              run: |
                echo "cloning repo.."
                echo "${{toJson(github)}}"
                git clone "https://github.com/${{github.repository}}.git"