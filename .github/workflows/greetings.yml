name: Greetings
# on: push
on:
    # - push
    - workflow_dispatch
jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - name: "great user"
              run: echo "heloo world"